-- Ejecutar UNA sola vez en pgAdmin (Query Tool conectado a la base "postgres").
-- Crea la base de datos vacia que usa el proyecto.
CREATE DATABASE instrumentos_db
    WITH ENCODING = 'UTF8'
    TEMPLATE = template0;
